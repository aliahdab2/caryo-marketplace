# Local development with real Gmail SMTP
# To use this profile, run with: -Dspring.profiles.active=local

# Mailpit SMTP Configuration (for testing)
spring.mail.host=localhost
spring.mail.port=1025
spring.mail.username=
spring.mail.password=
spring.mail.properties.mail.smtp.auth=false
spring.mail.properties.mail.smtp.starttls.enable=false
spring.mail.properties.mail.smtp.starttls.required=false

# Application Email Settings
app.email.from=ahdabeslov@gmail.com
app.website.name=Caryo Marketplace
app.website.url=http://localhost:3000

# Server Configuration
server.port=8082

# To use this profile, run: ./gradlew bootRun -Dspring.profiles.active=local
