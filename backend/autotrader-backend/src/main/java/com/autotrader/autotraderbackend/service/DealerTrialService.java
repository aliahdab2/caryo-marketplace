package com.autotrader.autotraderbackend.service;import com.autotrader.autotraderbackend.model.Dealer;import com.autotrader.autotraderbackend.repository.DealerRepository;import lombok.Builder;import lombok.Data;import lombok.RequiredArgsConstructor;import lombok.extern.slf4j.Slf4j;import org.springframework.beans.factory.annotation.Value;import org.springframework.stereotype.Service;import org.springframework.transaction.annotation.Transactional;import java.time.ZoneId;import java.time.ZonedDateTime;import java.time.temporal.ChronoUnit;/** * Service for managing dealer trial periods and subscription limits. * * Business Rules: * - Trial duration: 2 months * - Trial listing limit: 15 total listings * - Grace period: 3 days after trial expiry * - All calculations in UTC, display in dealer's timezone */@Service@Slf4j@RequiredArgsConstructorpublic class DealerTrialService {    private final DealerRepository dealerRepository;    @Value("${dealer.trial.duration_months:2}")    private int trialDurationMonths;    @Value("${dealer.trial.listing_limit:15}")    private int trialListingLimit;    @Value("${dealer.trial.grace_period_days:3}")    private int gracePeriodDays;    // Subscription tier limits    @Value("${subscription.basic.listing_limit:100}")    private int basicListingLimit;    @Value("${subscription.advanced.listing_limit:250}")    private int advancedListingLimit;    @Value("${subscription.professional.listing_limit:-1}")    private int professionalListingLimit; // -1 = unlimited    /**     * Check if dealer's trial is still active.     *     * @param dealer The dealer to check     * @return true if trial is active (not expired by time or listings)     */    public boolean isTrialActive(Dealer dealer) {        if (dealer.getTrialExpired()) {            return false;        }        if (dealer.getSubscriptionTier() != null && !"trial".equals(dealer.getSubscriptionTier())) {            return false;        }        ZonedDateTime trialEnd = getTrialEndDate(dealer);        ZonedDateTime now = ZonedDateTime.now(ZoneId.of("UTC"));        return now.isBefore(trialEnd);    }    /**     * Check if dealer can create a new listing.     *     * @param dealer The dealer attempting to create a listing     * @return true if dealer can create listing     */    public boolean canCreateListing(Dealer dealer) {        // Check if dealer is allowed to create listings (feature flag)        if (!dealer.getCanCreateListings()) {            log.warn("Dealer {} blocked from creating listings (feature flag)", dealer.getId());            return false;        }        // If on trial, check trial limits        if (isTrialActive(dealer)) {            boolean underLimit = dealer.getTrialListingsCount() < trialListingLimit;            if (!underLimit) {                log.info("Dealer {} hit trial listing limit: {}/{}",                    dealer.getId(), dealer.getTrialListingsCount(), trialListingLimit);            }            return underLimit;        }        // Check grace period        TrialStatus status = getTrialStatus(dealer);        if (status.isInGracePeriod()) {            boolean underLimit = dealer.getTrialListingsCount() < trialListingLimit;            log.info("Dealer {} in grace period, can create: {}", dealer.getId(), underLimit);            return underLimit;        }        // If has active subscription, check subscription limits        if (dealer.hasActiveSubscription()) {            return checkSubscriptionLimit(dealer);        }        // Trial expired and no subscription        log.warn("Dealer {} cannot create listing: trial expired, no subscription", dealer.getId());        return false;    }    /**     * Increment dealer's trial listing counter.     *     * @param dealer The dealer who created a listing     */    @Transactional    public void incrementListingCount(Dealer dealer) {        if (dealer.isOnTrial()) {            dealer.setTrialListingsCount(dealer.getTrialListingsCount() + 1);            dealerRepository.save(dealer);            log.info("Dealer {} trial listing count: {}/{}",                dealer.getId(), dealer.getTrialListingsCount(), trialListingLimit);        }    }    /**     * Get comprehensive trial status for a dealer.     *     * @param dealer The dealer to check     * @return TrialStatus with all relevant information     */    public TrialStatus getTrialStatus(Dealer dealer) {        ZonedDateTime now = ZonedDateTime.now(ZoneId.of("UTC"));        ZonedDateTime trialEnd = getTrialEndDate(dealer);        ZonedDateTime graceEnd = trialEnd.plusDays(gracePeriodDays);        long daysRemaining = ChronoUnit.DAYS.between(now, trialEnd);        boolean active = now.isBefore(trialEnd) && !dealer.getTrialExpired();        boolean inGracePeriod = now.isAfter(trialEnd) && now.isBefore(graceEnd);        int listingsRemaining = trialListingLimit - dealer.getTrialListingsCount();        double usagePercent = (dealer.getTrialListingsCount() * 100.0) / trialListingLimit;        // Determine if dealer can create listings (avoid calling canCreateListing to prevent recursion)        boolean canCreate = dealer.getCanCreateListings();        if (canCreate) {            // Check if within trial period or has active subscription            boolean withinPeriod = active || inGracePeriod || dealer.hasActiveSubscription();            // Check if under listing limit            boolean underLimit = dealer.getTrialListingsCount() < trialListingLimit;            canCreate = withinPeriod && underLimit;        }        return TrialStatus.builder()            .active(active)            .daysRemaining((int) Math.max(0, daysRemaining))            .listingsUsed(dealer.getTrialListingsCount())            .listingsRemaining(Math.max(0, listingsRemaining))            .listingsLimit(trialListingLimit)            .usagePercent(usagePercent)            .expiresAt(trialEnd)            .inGracePeriod(inGracePeriod)            .graceEndsAt(inGracePeriod ? graceEnd : null)            .canCreateListings(canCreate)            .subscriptionTier(dealer.getSubscriptionTier())            .subscriptionStatus(dealer.getSubscriptionStatus())            .build();    }    /**     * Get trial expiry date in dealer's local timezone.     *     * @param dealer The dealer     * @return Trial expiry time in dealer's timezone     */    public ZonedDateTime getTrialExpiryInDealerTimezone(Dealer dealer) {        ZonedDateTime trialEndUTC = getTrialEndDate(dealer);        String dealerTimezone = dealer.getTimezone() != null ? dealer.getTimezone() : "Asia/Damascus";        return trialEndUTC.withZoneSameInstant(ZoneId.of(dealerTimezone));    }    /**     * Extend trial for a dealer (support/admin use case).     *     * @param dealer The dealer     * @param additionalDays Number of days to extend     * @param reason Reason for extension (audit trail)     */    @Transactional    public void extendTrial(Dealer dealer, int additionalDays, String reason) {        ZonedDateTime currentTrialEnd = getTrialEndDate(dealer);        ZonedDateTime newTrialEnd = currentTrialEnd.plusDays(additionalDays);        dealer.setTrialExtendedUntil(newTrialEnd);        dealer.setTrialExpired(false); // Re-activate if expired        dealerRepository.save(dealer);        log.info("Extended trial for dealer {} by {} days. Reason: {}. New end: {}",            dealer.getId(), additionalDays, reason, newTrialEnd);        // Note: In production, also send notification email    }    /**     * Mark trial as expired (called by scheduled job).     *     * @param dealer The dealer whose trial expired     */    @Transactional    public void expireTrial(Dealer dealer) {        dealer.setTrialExpired(true);        dealerRepository.save(dealer);        log.info("Expired trial for dealer {}", dealer.getId());    }    /**     * Calculate trial end date (UTC).     *     * @param dealer The dealer     * @return Trial end date in UTC     */    private ZonedDateTime getTrialEndDate(Dealer dealer) {        ZonedDateTime trialStart = dealer.getTrialStartedAt() != null            ? dealer.getTrialStartedAt().withZoneSameInstant(ZoneId.of("UTC"))            : ZonedDateTime.now(ZoneId.of("UTC"));        // Check if trial was extended        if (dealer.getTrialExtendedUntil() != null) {            return dealer.getTrialExtendedUntil().withZoneSameInstant(ZoneId.of("UTC"));        }        return trialStart.plusMonths(trialDurationMonths);    }    /**     * Check if dealer can create listings based on subscription tier.     *     * @param dealer The dealer     * @return true if under subscription limit     */    private boolean checkSubscriptionLimit(Dealer dealer) {        String tier = dealer.getSubscriptionTier();        // For now, we'll just check if they have an active subscription        // In future, track total active listings and compare to tier limits        switch (tier) {            case "basic":                // TODO: Count active listings and compare to basicListingLimit                return true; // Simplified for Phase 1A            case "advanced":                // TODO: Count active listings and compare to advancedListingLimit                return true;            case "professional":                return true; // Unlimited            case "trial":                return isTrialActive(dealer) &&                       dealer.getTrialListingsCount() < trialListingLimit;            case "suspended":                return false;            default:                log.warn("Unknown subscription tier for dealer {}: {}", dealer.getId(), tier);                return false;        }    }    /**     * DTO for trial status information.     */    @Data    @Builder    public static class TrialStatus {        private boolean active;        private int daysRemaining;        private int listingsUsed;        private int listingsRemaining;        private int listingsLimit;        private double usagePercent;        private ZonedDateTime expiresAt;        private boolean inGracePeriod;        private ZonedDateTime graceEndsAt;        private boolean canCreateListings;        private String subscriptionTier;        private String subscriptionStatus;    }}